# Datasets

Raw datasets for dojo drills and lessons.

Most drills use built-in R datasets (`pistonrings`, `orangejuice`, `circuit` from `qcc`; `airquality`, etc.) or inline `tribble()` definitions.

Add external `.csv` or `.xlsx` files here when drills require them.

| File | Used by | Source |
|------|---------|--------|
| (none yet) | | |
